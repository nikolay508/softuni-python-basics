hour = int(input())
minutes = int(input())
minutes = minutes + 15

if minutes > 59:
    minutes = minutes % 60
    hour = hour + 1

if minutes < 10:
    minutes = "0" + str(minutes)

if hour > 23:
    hour = 0

print(f"{hour}:{minutes}")
