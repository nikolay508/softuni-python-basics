first_number = int(input())
second_number = int(input())
flag = False

for num in range(first_number, second_number + 1):
    num = str(num)
    evens = int(num[1]) + int(num[3]) + int(num[5])
    odds = int(num[0]) + int(num[2]) + int(num[4])
    if evens == odds:
        print(num, end=" ")
        continue
